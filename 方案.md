# 气象雷达数据管理与预测平台 - 完整技术方案

## 📋 项目概述

### 项目背景
构建一个完整的气象雷达数据管理与预测平台，实现从数据自动采集、处理存储、Web管理到智能预测的全流程自动化系统。

### 核心目标
1. **数据自动化**：每6分钟自动下载最新雷达图片
2. **数据标准化**：将雷达图片转换为站点时间序列数据
3. **管理可视化**：提供Web界面进行站点管理和数据查询
4. **预测智能化**：基于光流法和时序模型进行未来4-6小时趋势预测

### 部署环境
- **部署方式**：本地化部署
- **并发用户**：10人
- **数据库**：MySQL 8.0 (端口3308)
- **数据保留**：历史数据全部保留

---

## 🏗️ 系统架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端展示层                            │
│  Vue.js 3 + Element Plus + ECharts + Pinia                  │
│  - 站点管理  - 数据查询  - 预测展示  - 系统监控              │
└─────────────────────────────────────────────────────────────┘
                              ↓ HTTP/WebSocket
┌─────────────────────────────────────────────────────────────┐
│                       API网关层                              │
│  FastAPI + CORS + JWT认证 + 请求限流                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层                              │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │数据处理引擎│ │预测引擎   │ │任务调度器 │ │API服务    │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据持久层                              │
│  MySQL 8.0 (业务数据 + 时序数据 + 文件存储)                 │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈选型

| 层级 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| **前端框架** | Vue.js | 3.3+ | Composition API, 性能更优 |
| **UI组件库** | Element Plus | Latest | 企业级后台组件 |
| **状态管理** | Pinia | Latest | Vue 3官方推荐 |
| **图表库** | ECharts | 5.4+ | 强大的数据可视化 |
| **后端框架** | FastAPI | 0.100+ | 高性能异步框架 |
| **ORM** | SQLAlchemy | 2.0+ | Python最成熟的ORM |
| **任务队列** | Celery + Redis | 5.3+ | 异步任务处理 |
| **定时任务** | APScheduler | 3.10+ | 定时数据下载 |
| **数据库** | MySQL | 8.0+ | 关系型数据库 |
| **数据处理** | Pandas + NumPy | Latest | 数据分析处理 |
| **图像处理** | OpenCV | 4.8+ | 光流法实现 |
| **预测模型** | Prophet | Latest | 时序预测 |
| **API文档** | Swagger/ReDoc | - | 自动生成API文档 |

---

## 📦 核心功能模块

### 1. 数据下载模块 (Download Service)

#### 功能描述
- 自动每6分钟从中国气象局接口下载最新雷达图片
- 支持断点续传（记录已下载的文件列表）
- 失败自动重试3次，失败后跳过继续下载其他
- 下载完成后自动触发数据处理流程

#### 技术实现
```python
# 核心组件
- RadarImageDownloader: 图片下载器
- DownloadTaskManager: 任务管理器
- RetryPolicy: 重试策略
- DownloadLogger: 下载日志
```

#### 下载流程
```
1. 定时任务触发 (每6分钟)
2. 构建下载URL (基于时间戳)
3. 检查文件是否已下载 (断点续传)
4. 执行下载 (支持重试)
5. 保存到本地文件系统
6. 记录下载日志到数据库
7. 触发数据处理任务
```

#### 配置参数
```python
DOWNLOAD_CONFIG = {
    'interval_minutes': 6,          # 下载间隔
    'base_url': 'https://image.data.cma.cn/vis/RAD__B0_CR',
    'max_retries': 3,               # 最大重试次数
    'timeout': 30,                  # 下载超时(秒)
    'save_dir': './data/raw',       # 保存目录
    'enable_resume': True,          # 启用断点续传
}
```

---

### 2. 数据处理模块 (Processing Service)

#### 功能描述
- 根据定义的站点经纬度，从雷达图片中提取该站点的dBZ值
- 处理所有站点的数据（6分钟粒度）
- 数据质量检查与清洗
- 自动入库存储（区分实际数据和预测数据）

#### 技术实现
```python
# 核心组件
- CoordinateMapper: 坐标映射器 (经纬度 ↔ 像素坐标)
- ColorScaleParser: 色标解析器 (RGB → dBZ)
- RadarDataExtractor: 雷达数据提取器
- DataQualityChecker: 数据质量检查器
- DatabaseSaver: 数据库保存器
```

#### 处理流程
```
1. 接收新下载的雷达图片
2. 加载所有启用的站点配置
3. 对每个站点：
   a. 经纬度转像素坐标
   b. 提取RGB值
   c. 转换为dBZ值
   d. 质量检查
   e. 保存到数据库
4. 生成处理报告
5. 更新站点数据统计
```

#### 数据质量规则
```python
QUALITY_RULES = {
    'dbz_min': -30,                # dBZ最小有效值
    'dbz_max': 75,                 # dBZ最大有效值
    'max_missing_ratio': 0.3,      # 最大缺失比例
    'outlier_detection': True,     # 启用异常值检测
    'interpolation': 'linear',     # 插值方法
}
```

---

### 3. Web管理平台 (Admin Dashboard)

#### 3.1 站点管理模块

**功能列表**
- 站点列表查询（分页、搜索、筛选）
- 添加新站点（名称、经纬度、描述）
- 编辑站点信息
- 删除站点（软删除）
- 启用/禁用站点
- 批量导入站点（CSV/Excel）

**数据字段**
```python
Site = {
    'id': int,                    # 站点ID
    'name': str,                  # 站点名称
    'code': str,                  # 站点编码（唯一）
    'longitude': float,           # 经度
    'latitude': float,            # 纬度
    'altitude': float,            # 海拔高度（可选）
    'region': str,                # 所属区域
    'description': str,           # 描述信息
    'is_active': bool,            # 是否启用
    'created_at': datetime,       # 创建时间
    'updated_at': datetime,       # 更新时间
}
```

#### 3.2 数据查询模块

**功能列表**
- 选择站点
- 自定义时间范围查询
- 数据展示（表格 + 图表）
- 数据统计（最大值、最小值、平均值、标准差）
- 数据导出（CSV格式）

**查询界面要素**
```
┌─────────────────────────────────────────────┐
│ 站点选择: [下拉框]                           │
│ 时间范围: [开始日期] - [结束日期]           │
│ 数据类型: [实际数据] [预测数据] [全部]      │
│ 查询按钮: [查询] [导出CSV]                  │
└─────────────────────────────────────────────┘
```

**展示内容**
- 时间序列折线图（dBZ值随时间变化）
- 统计信息卡片
- 数据表格（支持排序、分页）
- 数据质量标识

#### 3.3 数据处理模块

**功能列表**
- 选择站点
- 选择时间范围
- 触发数据处理任务
- 查看处理进度
- 查看处理日志

**处理任务流程**
```
用户提交任务 → 创建后台任务 → 异步处理 → 实时进度更新 → 完成通知
```

#### 3.4 系统监控模块

**监控指标**
- 下载任务状态
- 数据处理进度
- 系统资源使用（CPU、内存、磁盘）
- 数据库连接状态
- 最新数据时间戳

**日志查看**
- 下载日志
- 处理日志
- 错误日志
- 系统日志

---

### 4. 预测引擎 (Prediction Engine)

#### 4.1 预测方法

**方法一：光流法追踪云团运动**

原理：使用OpenCV的Farneback光流法计算连续雷达图片之间的运动场，预测云团移动方向和速度。

```python
# 光流法实现流程
1. 读取连续N张雷达图片 (例如最近6张，即30分钟数据)
2. 转换为灰度图
3. 使用calcOpticalFlowFarneback计算稠密光流
4. 统计站点周围区域的运动矢量
5. 预测未来位置 (当前位置 + 运动矢量 × 时间步长)
6. 外推未来4-6小时的dBZ值
```

**方法二：Prophet时序模型**

原理：使用Facebook开源的Prophet时序预测模型，基于历史数据训练，预测未来趋势。

```python
# Prophet模型流程
1. 收集站点历史数据 (至少7天数据)
2. 数据预处理 (填充缺失值、异常值处理)
3. 训练Prophet模型
4. 预测未来4-6小时
5. 计算置信区间 (默认80%)
6. 保存预测结果
```

#### 4.2 预测结果输出

**预测数据字段**
```python
Prediction = {
    'id': int,                    # 预测ID
    'site_id': int,               # 站点ID
    'prediction_time': datetime,  # 预测时间点
    'predicted_dbz': float,       # 预测的dBZ值
    'confidence_lower': float,    # 置信区间下限
    'confidence_upper': float,    # 置信区间上限
    'model_type': str,            # 模型类型 ('optical_flow' 或 'prophet')
    'model_version': str,         # 模型版本
    'prediction_horizon': int,    # 预测时长(分钟)
    'created_at': datetime,       # 创建时间
}
```

#### 4.3 预测展示

**前端展示内容**
- 实际数据 vs 预测数据对比图
- 置信区间阴影图
- 预测准确度评估
- 模型信息展示

#### 4.4 模型更新策略

**定期训练计划**
```python
MODEL_UPDATE_CONFIG = {
    'prophet': {
        'retrain_interval': 'daily',      # 每天重新训练
        'min_data_points': 168,            # 至少7天数据 (7*24小时)
        'training_window': '30d',          # 使用最近30天数据训练
    },
    'optical_flow': {
        'calculation_interval': '6min',    # 每6分钟计算一次
        'history_frames': 6,               # 使用最近6帧
        'prediction_horizon': 360,         # 预测未来360分钟 (6小时)
    }
}
```

---

## 💾 数据库设计

### 数据库连接配置

```python
MYSQL_CONFIG = {
    'host': 'localhost',
    'port': 3308,
    'user': 'admin',
    'password': 'cqsyyxydxsyc6z',
    'database': 'gfs_weather',
    'charset': 'utf8mb4',
    'pool_size': 20,              # 连接池大小
    'max_overflow': 40,           # 最大溢出连接数
    'pool_recycle': 3600,         # 连接回收时间(秒)
    'echo': False,               # 是否打印SQL
}
```

### 核心表结构

#### 1. 站点表 (sites)

```sql
CREATE TABLE sites (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '站点ID',
    name VARCHAR(100) NOT NULL COMMENT '站点名称',
    code VARCHAR(50) UNIQUE NOT NULL COMMENT '站点编码',
    longitude DECIMAL(10, 6) NOT NULL COMMENT '经度',
    latitude DECIMAL(10, 6) NOT NULL COMMENT '纬度',
    altitude DECIMAL(8, 2) COMMENT '海拔高度(米)',
    region VARCHAR(100) COMMENT '所属区域',
    description TEXT COMMENT '描述信息',
    is_active TINYINT(1) DEFAULT 1 COMMENT '是否启用(0:禁用 1:启用)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME COMMENT '删除时间(软删除)',

    INDEX idx_region (region),
    INDEX idx_is_active (is_active),
    INDEX idx_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='站点信息表';
```

#### 2. 雷达图片表 (radar_images)

```sql
CREATE TABLE radar_images (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '图片ID',
    filename VARCHAR(255) NOT NULL COMMENT '文件名',
    file_path VARCHAR(500) NOT NULL COMMENT '文件路径',
    file_size BIGINT COMMENT '文件大小(字节)',
    observation_time DATETIME NOT NULL COMMENT '观测时间',
    download_time DATETIME COMMENT '下载完成时间',
    download_status ENUM('pending', 'success', 'failed', 'retrying') DEFAULT 'pending' COMMENT '下载状态',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    md5_hash VARCHAR(32) COMMENT '文件MD5值',
    is_processed TINYINT(1) DEFAULT 0 COMMENT '是否已处理',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    UNIQUE KEY uk_observation_time (observation_time),
    INDEX idx_download_status (download_status),
    INDEX idx_is_processed (is_processed),
    INDEX idx_observation_time (observation_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='雷达图片元数据表';
```

#### 3. 站点雷达数据表 (site_radar_data)

```sql
CREATE TABLE site_radar_data (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '数据ID',
    site_id INT NOT NULL COMMENT '站点ID',
    observation_time DATETIME NOT NULL COMMENT '观测时间',
    dbz_value DECIMAL(6, 2) COMMENT 'dBZ值',
    dbz_category ENUM('no_data', 'weak', 'moderate', 'strong', 'severe', 'extreme') COMMENT 'dBZ等级',
    cloud_impact_factor DECIMAL(4, 3) COMMENT '云影响因子(0-1)',
    rgb_value VARCHAR(20) COMMENT '原始RGB值',
    data_quality ENUM('good', 'interpolated', 'outlier', 'missing') DEFAULT 'good' COMMENT '数据质量',
    data_source ENUM('actual', 'predicted') DEFAULT 'actual' COMMENT '数据来源',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    UNIQUE KEY uk_site_time (site_id, observation_time),
    INDEX idx_observation_time (observation_time),
    INDEX idx_site_id (site_id),
    INDEX idx_data_source (data_source),
    FOREIGN KEY (site_id) REFERENCES sites(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='站点雷达数据表';
```

#### 4. 预测数据表 (site_predictions)

```sql
CREATE TABLE site_predictions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '预测ID',
    site_id INT NOT NULL COMMENT '站点ID',
    prediction_time DATETIME NOT NULL COMMENT '预测时间点',
    predicted_dbz DECIMAL(6, 2) NOT NULL COMMENT '预测的dBZ值',
    confidence_lower DECIMAL(6, 2) COMMENT '置信区间下限',
    confidence_upper DECIMAL(6, 2) COMMENT '置信区间上限',
    model_type ENUM('optical_flow', 'prophet', 'ensemble') NOT NULL COMMENT '模型类型',
    model_version VARCHAR(50) COMMENT '模型版本',
    prediction_horizon INT NOT NULL COMMENT '预测时长(分钟)',
    prediction_accuracy DECIMAL(5, 4) COMMENT '预测准确度(0-1)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_site_prediction_time (site_id, prediction_time),
    INDEX idx_model_type (model_type),
    INDEX idx_created_at (created_at),
    FOREIGN KEY (site_id) REFERENCES sites(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='预测数据表';
```

#### 5. 下载任务表 (download_tasks)

```sql
CREATE TABLE download_tasks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '任务ID',
    task_type ENUM('scheduled', 'manual', 'retry') NOT NULL COMMENT '任务类型',
    target_time DATETIME NOT NULL COMMENT '目标下载时间',
    status ENUM('pending', 'running', 'success', 'failed', 'cancelled') DEFAULT 'pending' COMMENT '任务状态',
    url VARCHAR(500) COMMENT '下载URL',
    save_path VARCHAR(500) COMMENT '保存路径',
    error_message TEXT COMMENT '错误信息',
    retry_count INT DEFAULT 0 COMMENT '重试次数',
    started_at DATETIME COMMENT '开始时间',
    completed_at DATETIME COMMENT '完成时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_status (status),
    INDEX idx_target_time (target_time),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='下载任务记录表';
```

#### 6. 模型版本表 (model_versions)

```sql
CREATE TABLE model_versions (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '模型ID',
    model_type ENUM('optical_flow', 'prophet', 'ensemble') NOT NULL COMMENT '模型类型',
    model_version VARCHAR(50) NOT NULL COMMENT '版本号',
    model_params JSON COMMENT '模型参数',
    training_start_time DATETIME COMMENT '训练开始时间',
    training_end_time DATETIME COMMENT '训练结束时间',
    training_samples INT COMMENT '训练样本数',
    model_metrics JSON COMMENT '模型评估指标',
    model_file_path VARCHAR(500) COMMENT '模型文件路径',
    is_active TINYINT(1) DEFAULT 0 COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    UNIQUE KEY uk_model_version (model_type, model_version),
    INDEX idx_model_type (model_type),
    INDEX idx_is_active (is_active)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='模型版本管理表';
```

#### 7. 处理日志表 (processing_logs)

```sql
CREATE TABLE processing_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    log_type ENUM('download', 'processing', 'prediction', 'system') NOT NULL COMMENT '日志类型',
    log_level ENUM('DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL') NOT NULL COMMENT '日志级别',
    message TEXT NOT NULL COMMENT '日志消息',
    details JSON COMMENT '详细信息',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_log_type (log_type),
    INDEX idx_log_level (log_level),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='处理日志表';
```

### 数据库优化策略

#### 分区表设计
对于大量时序数据，建议按时间分区：

```sql
-- 对 site_radar_data 表按月分区
ALTER TABLE site_radar_data
PARTITION BY RANGE (TO_DAYS(observation_time)) (
    PARTITION p202401 VALUES LESS THAN (TO_DAYS('2024-02-01')),
    PARTITION p202402 VALUES LESS THAN (TO_DAYS('2024-03-01')),
    -- ... 继续添加分区
    PARTITION p_future VALUES LESS THAN MAXVALUE
);
```

#### 索引优化
```sql
-- 复合索引优化查询
CREATE INDEX idx_site_time_source ON site_radar_data(site_id, observation_time, data_source);

-- 覆盖索引优化统计查询
CREATE INDEX idx_site_observation_dbz ON site_radar_data(site_id, observation_time, dbz_value);
```

---

## 🔌 API接口设计

### RESTful API规范

**基础URL**: `http://localhost:8000/api/v1`

**通用响应格式**
```json
{
    "code": 200,
    "message": "success",
    "data": {},
    "timestamp": "2024-03-10T16:00:00Z"
}
```

### 1. 站点管理接口

#### 1.1 获取站点列表
```
GET /sites
Query Parameters:
  - page: int (页码，默认1)
  - page_size: int (每页数量，默认20)
  - name: str (站点名称，模糊搜索)
  - region: str (区域筛选)
  - is_active: bool (是否启用)

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "items": [
            {
                "id": 1,
                "name": "北京站",
                "code": "BJ001",
                "longitude": 116.4074,
                "latitude": 39.9042,
                "region": "华北",
                "is_active": true,
                "created_at": "2024-03-10T00:00:00Z"
            }
        ],
        "total": 100,
        "page": 1,
        "page_size": 20
    }
}
```

#### 1.2 创建站点
```
POST /sites
Request Body:
{
    "name": "北京站",
    "code": "BJ001",
    "longitude": 116.4074,
    "latitude": 39.9042,
    "altitude": 50.0,
    "region": "华北",
    "description": "北京气象观测站"
}

Response:
{
    "code": 201,
    "message": "站点创建成功",
    "data": {
        "id": 1,
        "name": "北京站",
        ...
    }
}
```

#### 1.3 更新站点
```
PUT /sites/{site_id}
Request Body:
{
    "name": "北京站(更新)",
    "description": "更新后的描述"
}

Response:
{
    "code": 200,
    "message": "站点更新成功",
    "data": {...}
}
```

#### 1.4 删除站点
```
DELETE /sites/{site_id}

Response:
{
    "code": 200,
    "message": "站点删除成功"
}
```

### 2. 数据查询接口

#### 2.1 查询站点雷达数据
```
GET /sites/{site_id}/data
Query Parameters:
  - start_time: datetime (开始时间)
  - end_time: datetime (结束时间)
  - data_source: str (数据来源: 'actual', 'predicted', 'all')
  - page: int (页码)
  - page_size: int (每页数量)

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "site_info": {
            "id": 1,
            "name": "北京站",
            "longitude": 116.4074,
            "latitude": 39.9042
        },
        "statistics": {
            "count": 240,
            "max_dbz": 55.2,
            "min_dbz": 5.1,
            "avg_dbz": 28.3,
            "std_dbz": 12.5
        },
        "items": [
            {
                "observation_time": "2024-03-10T00:00:00Z",
                "dbz_value": 25.3,
                "dbz_category": "moderate",
                "cloud_impact_factor": 0.6,
                "data_quality": "good",
                "data_source": "actual"
            },
            ...
        ],
        "total": 240,
        "page": 1,
        "page_size": 20
    }
}
```

#### 2.2 导出数据为CSV
```
GET /sites/{site_id}/data/export
Query Parameters:
  - start_time: datetime
  - end_time: datetime
  - data_source: str

Response:
Content-Type: text/csv
Content-Disposition: attachment; filename="site_1_data_20240310.csv"

[CSV数据]
```

### 3. 预测接口

#### 3.1 获取站点预测数据
```
GET /sites/{site_id}/predictions
Query Parameters:
  - start_time: datetime
  - end_time: datetime
  - model_type: str ('optical_flow', 'prophet', 'all')

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "site_info": {...},
        "predictions": [
            {
                "prediction_time": "2024-03-10T17:00:00Z",
                "predicted_dbz": 30.5,
                "confidence_lower": 25.2,
                "confidence_upper": 35.8,
                "model_type": "prophet",
                "model_version": "v1.0.0",
                "prediction_horizon": 60
            },
            ...
        ]
    }
}
```

#### 3.2 触发预测任务
```
POST /sites/{site_id}/predict
Request Body:
{
    "model_type": "prophet",
    "prediction_horizon": 360  // 预测未来360分钟(6小时)
}

Response:
{
    "code": 202,
    "message": "预测任务已创建",
    "data": {
        "task_id": "task_123456",
        "status": "pending",
        "estimated_completion": "2024-03-10T16:05:00Z"
    }
}
```

### 4. 系统监控接口

#### 4.1 获取系统状态
```
GET /system/status

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "download_status": {
            "last_download_time": "2024-03-10T16:00:00Z",
            "next_download_time": "2024-03-10T16:06:00Z",
            "pending_tasks": 0,
            "failed_tasks": 0
        },
        "processing_status": {
            "last_processing_time": "2024-03-10T16:00:30Z",
            "pending_images": 0,
            "processing_queue_size": 0
        },
        "database_status": {
            "connection_pool_size": 20,
            "active_connections": 5,
            "status": "healthy"
        },
        "system_resources": {
            "cpu_usage": 25.5,
            "memory_usage": 45.2,
            "disk_usage": 60.8
        }
    }
}
```

#### 4.2 获取日志
```
GET /system/logs
Query Parameters:
  - log_type: str ('download', 'processing', 'prediction', 'system', 'all')
  - log_level: str ('DEBUG', 'INFO', 'WARNING', 'ERROR', 'CRITICAL')
  - start_time: datetime
  - end_time: datetime
  - page: int
  - page_size: int

Response:
{
    "code": 200,
    "message": "success",
    "data": {
        "items": [
            {
                "id": 1,
                "log_type": "download",
                "log_level": "INFO",
                "message": "雷达图片下载成功",
                "details": {...},
                "created_at": "2024-03-10T16:00:00Z"
            },
            ...
        ],
        "total": 1000,
        "page": 1,
        "page_size": 20
    }
}
```

---

## 🚀 项目实施计划

### 阶段一：基础架构搭建 (Week 1-2)

#### 后端搭建
- [x] 项目结构设计
- [x] FastAPI项目初始化
- [x] 数据库连接配置
- [x] SQLAlchemy模型定义
- [x] 基础API框架搭建
- [x] JWT认证系统
- [x] CORS配置

#### 前端搭建
- [x] Vue.js项目初始化
- [x] Element Plus集成
- [x] 路由配置
- [x] Axios封装
- [x] 登录页面
- [x] 主布局框架

### 阶段二：数据下载模块 (Week 3)

- [ ] 雷达图片下载器实现
- [ ] 断点续传功能
- [ ] 重试机制实现
- [ ] 定时任务配置
- [ ] 下载日志记录
- [ ] 下载状态监控

### 阶段三：数据处理模块 (Week 4)

- [ ] 坐标映射器实现
- [ ] 色标解析器实现
- [ ] 数据提取器实现
- [ ] 质量检查器实现
- [ ] 批量处理任务
- [ ] 数据库存储优化

### 阶段四：Web管理平台 (Week 5-6)

#### 站点管理
- [ ] 站点列表页面
- [ ] 站点添加/编辑对话框
- [ ] 站点删除功能
- [ ] 站点启用/禁用
- [ ] 批量导入功能

#### 数据查询
- [ ] 数据查询页面
- [ ] 时间范围选择器
- [ ] 数据表格展示
- [ ] ECharts图表集成
- [ ] 统计信息卡片
- [ ] CSV导出功能

#### 数据处理
- [ ] 数据处理任务创建
- [ ] 任务进度显示
- [ ] 处理日志查看

### 阶段五：预测引擎 (Week 7-8)

- [ ] 光流法模块实现
- [ ] Prophet模型集成
- [ ] 模型训练流程
- [ ] 预测结果存储
- [ ] 置信区间计算
- [ ] 模型版本管理
- [ ] 预测结果展示

### 阶段六：系统监控与优化 (Week 9)

- [ ] 系统监控仪表板
- [ ] 日志查看页面
- [ ] 性能监控
- [ ] 错误告警
- [ ] 数据库优化
- [ ] API性能优化

### 阶段七：测试与部署 (Week 10)

- [ ] 单元测试编写
- [ ] 集成测试
- [ ] 压力测试
- [ ] 部署脚本编写
- [ ] 部署文档编写
- [ ] 用户手册编写

---

## 📂 项目目录结构

```
radar_platform/
├── backend/                     # 后端项目
│   ├── app/
│   │   ├── api/                # API路由
│   │   │   ├── v1/
│   │   │   │   ├── endpoints/
│   │   │   │   │   ├── sites.py
│   │   │   │   │   ├── data.py
│   │   │   │   │   ├── predictions.py
│   │   │   │   │   └── system.py
│   │   │   │   └── __init__.py
│   │   ├── core/               # 核心配置
│   │   │   ├── config.py
│   │   │   ├── security.py
│   │   │   └── database.py
│   │   ├── models/             # 数据库模型
│   │   │   ├── site.py
│   │   │   ├── radar_image.py
│   │   │   ├── radar_data.py
│   │   │   ├── prediction.py
│   │   │   └── __init__.py
│   │   ├── schemas/            # Pydantic模型
│   │   │   ├── site.py
│   │   │   ├── data.py
│   │   │   └── prediction.py
│   │   ├── services/           # 业务逻辑
│   │   │   ├── download_service.py
│   │   │   ├── processing_service.py
│   │   │   ├── prediction_service.py
│   │   │   └── __init__.py
│   │   ├── tasks/              # 异步任务
│   │   │   ├── download_tasks.py
│   │   │   ├── processing_tasks.py
│   │   │   └── prediction_tasks.py
│   │   ├── utils/              # 工具函数
│   │   │   ├── radar_processor.py
│   │   │   ├── coordinate_mapper.py
│   │   │   ├── color_parser.py
│   │   │   └── optical_flow.py
│   │   ├── main.py            # FastAPI应用入口
│   │   └── __init__.py
│   ├── tests/                  # 测试文件
│   ├── alembic/               # 数据库迁移
│   ├── requirements.txt       # 依赖包
│   └── .env                   # 环境变量
│
├── frontend/                   # 前端项目
│   ├── src/
│   │   ├── api/               # API调用
│   │   ├── components/        # 组件
│   │   ├── views/             # 页面
│   │   │   ├── Sites/
│   │   │   ├── Data/
│   │   │   ├── Prediction/
│   │   │   └── System/
│   │   ├── store/             # Pinia状态管理
│   │   ├── router/            # 路由配置
│   │   ├── utils/             # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── package.json
│   └── vite.config.js
│
├── data/                       # 数据目录
│   ├── raw/                   # 原始雷达图片
│   ├── processed/             # 处理后的数据
│   └── models/                # 预测模型文件
│
├── logs/                       # 日志目录
│
├── scripts/                    # 脚本文件
│   ├── init_db.py             # 初始化数据库
│   ├── download_radar.py      # 手动下载脚本
│   └── train_model.py         # 模型训练脚本
│
├── docs/                       # 文档
│   ├── API.md                 # API文档
│   ├── DATABASE.md            # 数据库文档
│   └── DEPLOYMENT.md          # 部署文档
│
├── docker-compose.yml          # Docker配置(可选)
├── README.md                   # 项目说明
└── 方案.md                     # 本方案文档
```

---

## 🔐 安全与认证

### JWT认证机制

```python
# JWT配置
JWT_CONFIG = {
    'secret_key': 'your-secret-key-here',
    'algorithm': 'HS256',
    'access_token_expire_minutes': 60,  # 访问令牌过期时间
    'refresh_token_expire_days': 7,     # 刷新令牌过期时间
}
```

### API访问控制

```python
# 权限装饰器
@router.get("/sites/{site_id}")
@require_permission("site:read")
async def get_site(site_id: int):
    pass

@router.post("/sites")
@require_permission("site:write")
async def create_site(site_data: SiteCreate):
    pass
```

---

## ⚡ 性能优化策略

### 1. 数据库优化
- 连接池配置 (20-50连接)
- 读写分离 (主从复制)
- 分区表设计
- 索引优化
- 查询缓存 (Redis)

### 2. API性能优化
- 响应压缩 (gzip)
- 分页查询
- 异步处理 (Celery)
- 请求限流
- CDN加速 (静态资源)

### 3. 前端优化
- 代码分包
- 组件懒加载
- 虚拟滚动
- 图表数据抽样
- 防抖/节流

---

## 📊 监控与告警

### 监控指标

**系统指标**
- CPU使用率
- 内存使用率
- 磁盘使用率
- 网络流量

**业务指标**
- 下载成功率
- 处理完成率
- 预测准确度
- API响应时间

### 告警规则

```python
ALERT_RULES = {
    'download_failure_rate': {
        'threshold': 0.1,  # 失败率超过10%
        'action': 'email'
    },
    'database_connection_pool': {
        'threshold': 0.8,  # 连接池使用率超过80%
        'action': 'webhook'
    },
    'disk_usage': {
        'threshold': 0.9,  # 磁盘使用率超过90%
        'action': 'email'
    }
}
```

---

## 📝 开发规范

### 代码规范
- **Python**: 遵循PEP 8规范
- **JavaScript**: 遵循ESLint配置
- **命名**: 驼峰命名法(JS)、下划线命名法(Python)
- **注释**: 关键逻辑必须添加注释

### Git规范
```bash
# 提交信息格式
<type>(<scope>): <subject>

type:
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整
- refactor: 重构
- test: 测试相关
- chore: 构建/工具变动

示例:
feat(download): 添加断点续传功能
fix(api): 修复站点查询bug
docs(readme): 更新部署文档
```

### API文档规范
- 所有API使用Swagger自动生成文档
- 关键接口添加使用示例
- 错误码统一管理

---

## 🎯 项目里程碑

### 里程碑1：基础框架完成 (Week 2)
- ✅ FastAPI后端框架搭建完成
- ✅ Vue.js前端框架搭建完成
- ✅ 数据库表创建完成
- ✅ 基础API接口实现

### 里程碑2：数据模块完成 (Week 4)
- ✅ 自动下载功能上线
- ✅ 数据处理流程打通
- ✅ 数据自动入库

### 里程碑3：Web平台完成 (Week 6)
- ✅ 站点管理功能上线
- ✅ 数据查询功能上线
- ✅ 数据导出功能上线

### 里程碑4：预测功能完成 (Week 8)
- ✅ 光流法预测实现
- ✅ Prophet模型集成
- ✅ 预测结果展示

### 里程碑5：系统上线 (Week 10)
- ✅ 全功能测试完成
- ✅ 性能优化完成
- ✅ 文档完善
- ✅ 正式上线

---

## 💰 成本估算

### 开发成本
- **开发人员**: 1名全栈工程师 × 10周 = 2.5人月
- **测试人员**: 0.5人测试工程师 × 2周 = 0.25人月
- **总计**: 约3人月

### 运行成本
- **服务器**: 本地部署 (已有)
- **数据库**: MySQL (已有)
- **存储**: 预计每天雷达图片约240张 × 500KB = 120MB/天
- **带宽**: 下载流量约120MB/天

---

## 📞 联系与支持

### 技术支持
- **文档**: 见 `docs/` 目录
- **API文档**: 访问 `http://localhost:8000/docs`
- **问题反馈**: 创建Issue

### 更新日志
- **v1.0.0** (2024-03-10): 初始版本发布
- 后续版本将在此记录

---

## 📄 附录

### A. 环境变量配置示例 (.env)

```bash
# 数据库配置
DATABASE_HOST=localhost
DATABASE_PORT=3308
DATABASE_USER=admin
DATABASE_PASSWORD=cqsyyxydxsyc6z
DATABASE_NAME=gfs_weather

# JWT配置
JWT_SECRET_KEY=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# 下载配置
DOWNLOAD_BASE_URL=https://image.data.cma.cn/vis/RAD__B0_CR
DOWNLOAD_INTERVAL_MINUTES=6
DOWNLOAD_MAX_RETRIES=3
_DOWNLOAD_TIMEOUT=30

# 存储配置
DATA_DIR=./data
LOG_DIR=./logs
MODEL_DIR=./data/models

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Celery配置
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# 日志配置
LOG_LEVEL=INFO
LOG_FILE_MAX_SIZE=10MB
LOG_FILE_BACKUP_COUNT=5
```

### B. 启动命令

```bash
# 后端启动
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 前端启动
cd frontend
npm run dev

# Celery启动
cd backend
celery -A app.tasks.celery_app worker --loglevel=info

# Celery Beat启动 (定时任务)
celery -A app.tasks.celery_app beat --loglevel=info
```

---

**文档版本**: v1.0.0
**最后更新**: 2024-03-10
**作者**: 气象雷达数据平台团队
